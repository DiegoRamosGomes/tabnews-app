import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 16
  },
  emptyContainer: {
    alignItems: 'center',
    marginHorizontal: '20%'
  },
  emptyText: {
    textAlign: 'center'
  }
})